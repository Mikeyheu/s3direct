<%= s3_uploader_form post: images_url, as: "image[image_url]" do %>
  <%= file_field_tag :file, multiple: true %>
<% end %>

<div class="thumbnail_grid">
  <% @images.each do |image| %>
    <div class="thumbnail">
      <%= image_tag image.image_file_url %>
    </div>
  <% end %>
</div>
